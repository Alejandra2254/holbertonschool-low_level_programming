#include <stdio.h>
#include "holberton.h"
/**
 * main - prints
 * @argc: number of comand line arguments
 * @argv: An array
 * Return: 0
 */

int main(int argc, char *argv[])
{

	printf("%s\n", argv[0]);
	return (0);
}
