#include "holberton.h"

/**
 * mul -  checks for checks for a digit (0 through 9).
 * @a: first integer
 * @b: second integer
 * return: result
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
