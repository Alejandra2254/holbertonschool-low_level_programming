#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n, nl;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	last = n % 10;
        printf("las number %d \n",nl);
	return (0);
}
