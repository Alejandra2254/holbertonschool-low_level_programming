#include "holberton.h"

/**
 * _strcpy - copy string
 * @dest: return value
 * @src: imput
 * Return: 0 success
 */
char *_strcpy(char *dest, char *src)
{
	int i;

	for (i = 0; src[i]; i++)
	{
		src[i] = dest[i];
	}
	dest[i] = src[i];
	return (dest);
}
